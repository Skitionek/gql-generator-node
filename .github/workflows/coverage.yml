---
name: Coverage

"on":
  push:
    branches:
      - master
  pull_request: {}

permissions: read-all

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Install dependencies
        run: npm ci
      - name: Test coverage
        run: npm run cover

      - name: Notify Microsoft Teams
        uses: Skitionek/notify-microsoft-teams@740f7ff6d381f910f9a25d71789593478b3743d1
        if: failure()
        with:
          webhook_url: ${{ secrets.MSTEAMS_WEBHOOK }}
          needs: ${{ toJson(needs) }}
          job: ${{ toJson(job) }}
          steps: ${{ toJson(steps) }}
